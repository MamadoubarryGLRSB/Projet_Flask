// Get the flash message element
const flashMsg = document.getElementById('flash-message');

// Check if there is a flash message
if (flashMsg) {
  // Display the flash message
  alert(flashMsg.textContent);
}
